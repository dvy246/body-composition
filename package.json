{
  "name": "body-composition",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "ASTRO_DEV_BACKGROUND=0 astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && npx wrangler pages deploy dist --commit-dirty=true",
    "test": "node --experimental-strip-types --test src/lib/engines.test.ts"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/vite": "^4.3.1",
    "astro": "^7.0.2",
    "tailwindcss": "^4.3.1",
    "uipro-cli": "^2.2.3"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}